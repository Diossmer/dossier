import { DossierContainer } from './components/DossierContainer'

export default function App() {
  return (
    <main className="relative w-full min-h-screen bg-black overflow-hidden">
      <DossierContainer />
    </main>
  )
}
